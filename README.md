1. Clone the repository
   ```sh
   git clone https://github.com/Carbon32/n8-chatbot
   ```
2. Install NPM packages (run inside the client folder)
   ```sh
   npm install
   ```
3. Install Flask
   ```sh
   pip install flask
   ```